{
  "name": "ppr-auction",
  "version": "0.1.0",
  "private": true,
  "description": "Deflator — a live auction assistant built on the Deflators League valuation model.",
  "scripts": {
    "postinstall": "npm --prefix app install",
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "preview": "npm --prefix app run preview",
    "check": "npm --prefix app run check",
    "espn": "node tools/check-espn.mjs",
    "fake-espn": "node tools/fake-espn.mjs"
  },
  "engines": {
    "node": ">=22.12"
  }
}
